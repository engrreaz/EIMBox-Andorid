Profile
    My Profile  
        Upload/Change Photo 
    Attendance
        confused
    leaves & Movement
        page-info-box 
    
ALTER TABLE `whatsnew` ADD `guest` INT NOT NULL DEFAULT '0' AFTER `librarian`;
ALTER TABLE `leaveapp` CHANGE `id` `id` INT(11) NOT NULL AUTO_INCREMENT, add PRIMARY KEY (`id`);
ALTER TABLE `leaveapp` CHANGE `id` `id` INT(11) NOT NULL AUTO_INCREMENT, CHANGE `tid` `tid` INT(11) NULL DEFAULT NULL, CHANGE `sccode` `sccode` INT(11) NULL DEFAULT NULL, CHANGE `ldate` `ldate` DATE NULL DEFAULT NULL, CHANGE `applytime` `applytime` DATETIME on update CURRENT_TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP, CHANGE `entryby` `entryby` VARCHAR(30) CHARACTER SET utf8 COLLATE utf8_unicode_ci NULL DEFAULT NULL, CHANGE `status` `status` VARCHAR(20) CHARACTER SET utf8 COLLATE utf8_unicode_ci NULL DEFAULT NULL, CHANGE `leavetype` `leavetype` VARCHAR(20) CHARACTER SET utf8 COLLATE utf8_unicode_ci NULL DEFAULT NULL, CHANGE `reason` `reason` VARCHAR(100) CHARACTER SET utf8 COLLATE utf8_unicode_ci NULL DEFAULT NULL;
